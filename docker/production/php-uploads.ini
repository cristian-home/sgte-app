; Override defaults for bulk import uploads (admin-data-imports).
; FrankenPHP / dunglas/frankenphp:1-php8.5-trixie ships with the upstream
; PHP defaults of 2M / 8M / 128M which cap our intended 20MB cap.

upload_max_filesize = 25M
post_max_size = 30M
memory_limit = 256M
