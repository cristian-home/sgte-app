# PWA icons live here.
#
# TODO(pwa-icons): drop the following PNGs into this folder so installable PWA
# installs render correctly on Android, iOS and desktop Chromium browsers:
#
#   - pwa-192.png             192x192, any purpose (home screen / launcher)
#   - pwa-512.png             512x512, any purpose (splash screen)
#   - pwa-maskable-512.png    512x512, maskable purpose (Android adaptive icon,
#                              safe zone = central 80% circle)
#
# Closest existing references:
#   - public/apple-touch-icon.png  (current home-screen icon)
#   - public/favicon.svg           (vector source if available)
#
# Filenames must match the `icons` array in `vite.config.ts`.
